// place to put API keys, DB config, settings that can change from dev to prod
// TODO: set NODE_ENV env variable to 'production' when in prod then check here

module.exports = {
    secret: 'supersecretkey'
}